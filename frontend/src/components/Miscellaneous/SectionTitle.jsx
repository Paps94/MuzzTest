import React from "react";

const SectionTitle = (probs) => {
  return (
    <div class="text-[#FB406C] text-left text-3xl uppercase font-bold m-4 ml-0">
      {probs.title}
    </div>
  );
};

export default SectionTitle;
